"""
List of datatypes
"""

"""
IMMUTABLE TYPES

    IMMUTABLE means, Once we store values in a variable then that values can't be changed throughout the program

    1. Numbers: We already have option to store numbers like 10, 12,5 in a variables

    2. Strings:  We already have option to store text data like name, email-id, address, word, sentence, paragraphs
        - Automatically index number will be assigned to each and every characters

    3. Tuple: We already have option to store multiple values like list of employee names.
        - We can store DUPLICATE values
        - Automatically index number will be assigned to each and every values

    4. Frozenset: We already have option to store multiple values like list of employee names.
        - We can store UNIQUE values
        - We don't have index number to each values

MUTABLE TYPES

    MUTABLE means, Once we store values in a variable then that value can be changed throughout the program

    1. List: We already have option to store multiple values like list of employee names.
        - We can store DUPLICATE values
        - Automatically index number will be assigned to each and every values

    2. Set: We already have option to store multiple values like list of employee names.
        - We can store UNIQUE values
        - We don't have index number to each values

    3. Dictionary: We already have option to store multiple values like list of employee names.
        - We can store DUPLICATE values
        - MANUALLY We-can/we-have-to provide index to each value. KEY/VALUE pair

"""